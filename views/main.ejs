
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <link rel="stylesheet" href='main.css'>
    <link rel="stylesheet" href='https://fontawesome.com/v4.7.0/icon/bars'>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" />
    
<style>
    #type{
        width: 100%;
        height:500px
    }
</style>
</head>
<title>Online donation system</title>


<body background="Images\div.jpg">
    <!--Scroll to top button-->
    
    <!-- Home Page -->

       
        
        <div id="nav">
            <div class="header-list" id="headerl">
               
                <ul>
                    <li><a href="profile.ejs">profile</a></li>
                    <li><a href="donate2.html" target="_self">Donate</a></li>
                    <li><a href="help.html" target="_self">Get Help</a></li>
                    <li><a target="_self" href="/logout">logOut</a></li>
                    <li><a onclick="run()">delete Donation

                    </a></li>
                    
                </ul>
            </div>

            </div>
      
        <div id="head">
         <img id="type" src="Images\blood_type.png" >
        </div>
<input type="search" id="searchInput" placeholder="city name">
<input type="search" id="searchInput1" placeholder="blood_group">
<div id="ta">
  <table id="myTable">
<tr>
    <th id="Name">Name</th>
    <th id="email">Email</th>
    <th>Blood group</th>
    <th>phone no.</th>
    <th>city</th>
    <tbody>
        <tbody>
            <% s.forEach(item => { %>
                <tr>
                    <td><%= item.Name %></td>
                    <td><%= item.username %></td>
                    <td><%= item.blood %></td>
                    <td><%= item.phone %></td>
                    <td><%= item.city %></td>
                    <!-- Add more fields if needed -->
                </tr>
            <% }); %>
        </tbody>
</tr>
   </table>
   </div>
    <!--Javascript for pre-loader-->
    <script>


function run(){
 var output= confirm("Press a button!\nEither OK or Cancel.");
 console.log(output);
 if(output){
    window.location.href="/don";
 }
}


        document.getElementById("searchInput").addEventListener("keyup", function() {
            let input = this.value.toLowerCase();
            let table = document.getElementById("myTable");
            let rows = table.getElementsByTagName("tr");
            rows[0].style.display = ""
            for (let i = 1; i < rows.length; i++) {
                let cells = rows[i].getElementsByTagName("td");
                let found = false;
                for (let j = 0; j < cells.length; j++) {
                    let cellValue = cells[j].textContent || cells[j].innerText;
                    if (cellValue.toLowerCase().indexOf(input) > -1) {
                        found = true;
                        break;
                    }
                }
                if (found) {
                    rows[i].style.display = "";
                } else {
                    rows[i].style.display = "none";
                }
            }
        });
        document.getElementById("searchInput1").addEventListener("keyup", function() {
            let input = this.value.toLowerCase();
            let table = document.getElementById("myTable");
            let rows = table.getElementsByTagName("tr");
            rows[0].style.display = ""
            for (let i = 1; i < rows.length; i++) {
                let cells = rows[i].getElementsByTagName("td");
                let found = false;
                for (let j = 0; j < cells.length; j++) {
                    let cellValue = cells[j].textContent || cells[j].innerText;
                    if (cellValue.toLowerCase().indexOf(input) > -1) {
                        found = true;
                        break;
                    }
                }
                if (found) {
                    rows[i].style.display = "";
                } else {
                    rows[i].style.display = "none";
                }
            }
        });
    </script>
    
   
    
    <script>
        const preloader = document.querySelector('.preloader');
        const fadeEffect = {key:setInterval(() => {
            if (!preloader.style.opacity) {
                preloader.style.opacity = 1;
            }
            if (preloader.style.opacity > 0) {
                preloader.style.opacity -= 1.5;
            } else {
                clearInterval(fadeEffect);
            }
        }, 1500)}
        window.addEventListener('load', fadeEffect);
    </script>
    <!--js for scroll to top-->
    <script src="up.js"></script>

    <!--JAVASCRIPT FOR TOGGLE MENU -->
    <script>
        var headerl = document.getElementById("headerl");

        function showMenu() {
            headerl.style.right = "0";
        }

        function hideMenu() {
            headerl.style.right = "-210px";
        }
    </script>


    <!--js for scroll effects-->
    <script src="scroll.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
</body>

</html>